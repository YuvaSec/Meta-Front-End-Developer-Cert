# STEP 1 ( Make a function )
```js 
function listArrayItems(arr, prc){
//place for code.
}
```

# STEP 2 ( Creating Two Arrays )
```js
arr = ['red', 'orange', 'yellow', 'green', 'blue', 'purple', 'pink']
prc = [ 5, 6, 8, 10, 4, 5, 2 ]
```

# STEP 3 ( Loop in the function )
```js
function listArrayItems(arr, prc){
	for(let i=0; i<arr.length; i++){
		console.log(arr[i],prc[i]);
	}
}
```

# STEP 4 ( Combine all the elements )
```js
function listArrayItems(a, p){
	for(let i=0; i<arr.length; i++){
		console.log(a[i],p[i]);
	}
}
arr = ['red', 'orange', 'yellow', 'green', 'blue', 'purple', 'pink']
prc = [ 5, 6, 8, 10, 4, 5, 2 ]
listArrayItems(arr, prc);
```
